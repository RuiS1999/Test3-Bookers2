<h2>New book</h2>
  <%= form_with model: @book do |f| %>
    <div>
      <h4><%= f.label  :title %></h4>
      <%= f.text_field :title %>
    </div>
    <div>
      <h4><%= f.label :body %></h4>
      <%= f.text_area :body %>
    </div>
    <div><%= f.submit 'Create Book' %></div>
  <% end %>