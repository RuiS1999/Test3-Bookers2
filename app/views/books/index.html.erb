<div>
<h2>User info</h2>
<%= image_tag current_user.get_profile_image(100,100) %>
  <table>
    <tr></tr>
    <tr>
      <th>name</th>
      <th><%= current_user.name%></th>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= current_user.introduction %></th>
    </tr>
  </table>

  <div>
    <a class="fa-solid fa-user-gear"　href=""></a>
  </div>

<h2>New book</h2>
  <%= form_with model: @book do |f| %>
    <div>
      <h4><%= f.label  :title %></h4>
      <%= f.text_field :title %>
    </div>
    <div>
      <h4><%= f.label :body %></h4>
      <%= f.text_area :body %>
    </div>
    <div><%= f.submit 'Create Book' %></div>
  <% end %>
</div>


<div>
<h1>Books</h1>
<table>
  <thead>
    <tr>
      <th colspan = "1"></th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= image_tag book.user.get_profile_image(100,100) %></td>
        <td><%= link_to book.title,"/books/#{book.id}" %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>